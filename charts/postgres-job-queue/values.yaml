app:
  image:
    repository: postgres-job-queue
    tag: dev
    pullPolicy: IfNotPresent

postgres:
  image:
    repository: postgres
    tag: 16-alpine
    pullPolicy: IfNotPresent
  auth:
    user: queue
    password: queue
    database: queue
  service:
    port: 5432
  persistence:
    size: 1Gi
